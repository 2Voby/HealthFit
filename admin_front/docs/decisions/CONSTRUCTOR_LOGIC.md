Each question will be assigned with attributes like age, gender, goal, etc. that are specified in task. 
Each answer option will add attributes to the user profile.
Then admin can set up transitions between questions based on the user profile attributes. 
Then, the end result with the plan will be generated based on the user attributes.
