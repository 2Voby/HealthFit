FOr UI for the admin panel we decided to go with visual drag n drop constructor inspired by chatbot builders.
The main idea is to have a canvas where the user can drag and drop components: questions, answers, transitions and arrange them as they like. 
The attributes of each component can be edited inside the component itself.